'use client';
import {useEffect,useState} from 'react';
export default function SuppliersPage(){const [rows,setRows]=useState<any[]>([]);const [balances,setBalances]=useState<any[]>([]);useEffect(()=>{Promise.all([fetch('/api/v1/suppliers').then(r=>r.json()),fetch('/api/v1/suppliers/balances').then(r=>r.json())]).then(([a,b])=>{setRows(Array.isArray(a)?a:[]);setBalances(Array.isArray(b)?b:[])}).catch(()=>{})},[]);return <section><div className="flex items-center justify-between mb-6"><div><h1 className="text-2xl font-bold">Fournisseurs</h1><p className="text-sm text-gray-500">Factures, soldes et paiements fournisseurs.</p></div></div><div className="grid md:grid-cols-3 gap-4 mb-6"><div className="bg-white border rounded-xl p-5"><div className="text-xs text-gray-500">Fournisseurs actifs</div><div className="text-2xl font-bold mt-1">{rows.length}</div></div><div className="bg-white border rounded-xl p-5"><div className="text-xs text-gray-500">Factures impayées</div><div className="text-2xl font-bold mt-1">{balances.length}</div></div><div className="bg-white border rounded-xl p-5"><div className="text-xs text-gray-500">À payer</div><div className="text-2xl font-bold mt-1">{balances.reduce((s,x)=>s+Number(x.balance||0),0).toLocaleString('fr-MA')} MAD</div></div></div><div className="bg-white border rounded-xl overflow-hidden"><table className="w-full text-sm"><thead className="bg-gray-50"><tr><th className="text-left p-3">Fournisseur</th><th className="text-left p-3">ICE</th><th className="text-left p-3">Conditions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id} className="border-t"><td className="p-3 font-medium">{x.name}</td><td className="p-3">{x.ice||'—'}</td><td className="p-3">{x.paymentTermsDays} jours</td></tr>)}</tbody></table></div></section>}
